# TODO

- [x] Fix package.json JSON parse error so `npm run build` can run.

- [x] Run `npm run build` and capture TypeScript/ESLint errors.

- [ ] Run `npm run lint` (tsc noEmit) to get the full list of issues in `src/App.tsx` and related files.
- [ ] Patch `src/App.tsx` to resolve the reported ~18 problems.
- [ ] Re-run `npm run build` until it succeeds.

